const initialData = {
  cars: ["tata", "layland"],
};

const carsReducrs = (state = initialData, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default carsReducrs;
